# GROW v4.0 — configuration merge snippet.
# Merge these blocks into your existing /config/configuration.yaml if they are
# not already present. Do not duplicate top-level keys if you already have them.

homeassistant:
  packages: !include_dir_named packages/grow

lovelace:
  dashboards:
    grow-arbiter-v4:
      mode: yaml
      title: Grow v4.0
      icon: mdi:cog-box
      show_in_sidebar: true
      filename: lovelace/grow_dashboard.yaml

